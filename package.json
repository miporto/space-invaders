{
  "name": "space-invaders",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "spike": "bun run src/spike.ts",
    "spike:bun": "bun run src/spike.ts",
    "spike:node": "bunx tsc -p tsconfig.build.json && node dist/spike.js",
    "test": "vitest run",
    "lint": "oxlint src",
    "typecheck": "bunx tsc -p tsconfig.build.json --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^25.0.3",
    "oxlint": "^1.36.0",
    "vitest": "^4.0.16"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@opentui/core": "^0.1.67"
  }
}
